#include <iostream> 
#include <iomanip> 
#include "windows.h"

using namespace std;

int main()
{
	const n = 10;
	int A[n];
	cout << "Enter Mass:" << endl;
	for (int i = 0; i < n; i++)
	{
		cout << "A[" << i + 1 << "]=";
		cin >> A[i];
	}
	cout << "\n Mass:\n";
	for (int i = 0; i < n; i++)
		cout << setw(5) << A[i] << endl;

}